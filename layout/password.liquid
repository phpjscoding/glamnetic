<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% # Inlined CSS Variables %}
    {% render 'css-variables' %}

    {% # Load and preload the critical CSS %}
    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    {{ content_for_header }}
  </head>

  <body>
    {{ content_for_layout }}
  </body>
</html>

{% comment %}
   START HTML DOCUMENT (full height page)

  HEAD SECTION:
    - Set character encoding to UTF-8
    - Set viewport for mobile devices
    - Add canonical URL link

    IF favicon exists THEN
      - Add favicon link
    END IF

    IF using custom header font THEN
      - Connect to Shopify fonts server
    END IF

    TITLE:
      - Display shop name only
    END TITLE

    - Add page description meta tag
    - Load meta tags component
    - Load Shopify header content

    LOAD FONTS:
      - Get body font (normal, bold, italic, bold-italic)
      - Get header font
    END LOAD

    CREATE CSS VARIABLES:
      FOR each color scheme DO
        - Set background color (solid or gradient)
        - Set text color
        - Set shadow color
        - Set button colors (primary and secondary)
        - Set link color
        - Set badge colors
        - Set payment terms background
      END FOR

      SET body colors using selected scheme

      SET ROOT VARIABLES:
        FONTS:
          - Body font family, style, weight
          - Header font family, style, weight
          - Font scale calculations

        MEDIA ELEMENTS:
          - Padding, border, radius
          - Shadow properties (opacity, offset, blur)
          - Shadow visibility

        PAGE LAYOUT:
          - Page width
          - Page margin

        PRODUCT CARDS:
          - Image padding
          - Corner radius
          - Text alignment
          - Border width and opacity
          - Shadow properties

        COLLECTION CARDS:
          - Image padding
          - Corner radius
          - Text alignment
          - Border width and opacity
          - Shadow properties

        BLOG CARDS:
          - Image padding
          - Corner radius
          - Text alignment
          - Border width and opacity
          - Shadow properties

        BADGES:
          - Corner radius

        SPACING:
          - Section spacing (desktop and mobile)
          - Grid spacing (vertical and horizontal)
          IF mobile spacing < 24px THEN
            - Use same spacing
          ELSE
            - Calculate 70% of desktop spacing (minimum 20px)
          END IF

        TEXT BOXES:
          - Border opacity and width
          - Border radius
          - Shadow properties and visibility

        BUTTONS:
          - Border radius and outset
          - Border width (based on opacity)
          - Border opacity
          - Shadow properties and visibility
          - Border offset calculation

        INPUT FIELDS:
          - Border radius and outset
          - Border width and opacity
          - Shadow properties
          - Margin offset (based on shadow)

        VARIANT PILLS:
          - Border radius and width
          - Border opacity
          - Shadow properties
      END SET
    END CREATE

    PRELOAD FONTS:
      IF body font not system font THEN
        - Preload body font file
      END IF
      IF header font not system font THEN
        - Preload header font file
      END IF
    END PRELOAD

    LOAD CSS FILES:
      - section-password.css (password page styles)
      - base.css (base styles)
      - component-list-social.css (social media icons)
    END LOAD

    LOAD JAVASCRIPT FILES:
      - global.js (global functions)
      - details-modal.js (modal functionality)
      - password-modal.js (password form)
    END LOAD

  END HEAD SECTION

  BODY SECTION (password page with gradient):
    - Add skip to content link for accessibility

    - Render password page header section

    MAIN CONTENT AREA:
      - Display password page content
    END MAIN

    FOOTER:
      - Render password page footer section
    END FOOTER

    HIDDEN ACCESSIBILITY MESSAGES:
      - New window message
    END HIDDEN

  END BODY SECTION

  END HTML DOCUMENT
{% endcomment %}


